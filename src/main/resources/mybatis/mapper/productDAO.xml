<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.Pilotproject.dao.IproductDAO">
	<select id="productList" resultType="com.springboot.Pilotproject.dto.productDTO">
		select * from coffeeProduct order by pbest asc
	</select>
	<select id="productView" resultType="com.springboot.Pilotproject.dto.productDTO">
		select * from coffeeProduct where pno=#{pno}
	</select>
	<select id="productBest" resultType="com.springboot.Pilotproject.dto.productDTO">
		select * from coffeeProduct where pbest='best' order by pname asc
	</select>
	<insert id="productWrite">
		insert into coffeeProduct(pno, pbrand, pname, pimage, porigin, pgram, pflavor, proastingLevel, pcontent, pprice, pamount, pdate, mno, pbest)
		 values(coffeeProduct_seq.NEXTVAL, #{pbrand}, #{pname}, #{pimage}, #{porigin}, #{pgram}, #{pflavor, jdbcType=VARCHAR}, #{proastingLevel, jdbcType=VARCHAR}, #{pcontent, jdbcType=VARCHAR}, #{pprice}, #{pamount}, SYSDATE, #{mno}, #{pbest})
	</insert>
	<update id="productUpdate">
		update coffeeProduct
		 set pname=#{pname}, pimage=#{pimage}, porigin=#{porigin}, pgram=#{pgram}, pflavor=#{pflavor, jdbcType=VARCHAR}, proastingLevel=#{proastingLevel, jdbcType=VARCHAR}, pcontent=#{pcontent, jdbcType=VARCHAR}, pprice=#{pprice}, pamount=#{pamount}, pbest=#{pbest}
		 where pno=#{pno}
	</update>
	<delete id="productDelete">
		delete from coffeeProduct
		 where pno=#{pno}
	</delete>
</mapper>